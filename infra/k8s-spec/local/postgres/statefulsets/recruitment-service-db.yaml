apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: recruitment-service-db
spec:
  serviceName: postgres-recruitment-service
  replicas: 1
  selector:
    matchLabels:
      app: recruitment-service-db
  template:
    metadata:
      labels:
        app: recruitment-service-db
    spec:
      containers:
        - name: db
          image: postgres:16
          env:
          - name: POSTGRES_PASSWORD_FILE
            value: /secrets/postgres_password
          envFrom:
            - configMapRef:
                name: postgres-recruitment-service-config
          volumeMounts:
            - name: secret
              mountPath: "/secrets"
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: init-scripts
              mountPath: /docker-entrypoint-initdb.d
      volumes:
        - name: secret
          secret:
            secretName: postgres-secret
            defaultMode: 0400
            items:
            - key: POSTGRES_PASSWORD
              path: postgres_password
        - name: data
          persistentVolumeClaim:
            claimName: postgres-recruitment-pvc
        - name: init-scripts
          configMap:
            name: postgres-recruitment-init-script
            defaultMode: 0755